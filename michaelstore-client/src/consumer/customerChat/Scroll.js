import React from 'react'
import ScrollEmbed from './ScrollEmbed'

const Scroll = (props) => {
    return (
        <div>
            <ScrollEmbed scroll={props.scroll}/>
        </div>
    )
}

export default Scroll
